# Secrets — the local profile carries the Claude key and the Mongo connection string.
**/application-local.yml
.env
.env.local

# Java
backend/target/

# Node / Next.js
node_modules/
frontend/.next/
frontend/out/
frontend/node_modules/
*.tsbuildinfo
next-env.d.ts

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Excel lock files from the source data directory
~$*.xlsx

# HR Ops source workbooks — large data files, not source code
/PEOPLE_INSIGHTS_DASHBOARD_DATA/

# Keys and certs — never commit these (see incident: robin-dev.pem, Aug 2026)
*.pem
*.key
*.p12
*.pfx
id_rsa*
